# Variables de entorno de ejemplo
# Copia este archivo a `.env` y edita los valores antes de ejecutar localmente o con Docker.

# Required
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ceibus
JWT_SECRET=change-me

# Optional / recommended
BCRYPT_SALT_ROUNDS=10
JWT_EXPIRES_IN=30m
NODE_ENV=development
PORT=3000

